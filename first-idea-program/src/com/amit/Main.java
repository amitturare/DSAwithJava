package com.amit;

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        // Scanner is a class which allows to take inputs

         System.out.println(input.nextInt()); // Will allow to take integer as input
        // To input the first string until space comes, input.next()
        // To input the whole specified string, input.nextLine()
    }
}
